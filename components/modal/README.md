# AppModal Component

A reusable modal component for use inside screens.

- Location: `components/modal/index.tsx`
- Styles: defined locally in the same file
- Tests: placeholder in `components/modal/index.test.ts`
